---
apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: { { .Values.namespace } }
  labels:
    name: user-db
spec:
  type: ExternalName
  externalName: { { .Values.user.service.externalName } }
